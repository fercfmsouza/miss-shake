import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contact',
  description: 'Miss Shake contact',
};

export default function Contact() {
  return <div>contact page</div>;
}
